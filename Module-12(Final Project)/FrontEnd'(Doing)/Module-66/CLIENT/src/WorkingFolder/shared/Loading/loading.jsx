import React from 'react';

const Loading = () => {
    return (
        <div>
            <span className="loading loading-spinner loading-xs"></span>
        </div>
    );
};

export default Loading;